{
  "name": "ResQFlow",
  "install": "python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt && cp -n .env.example .env || true",
  "start": "./start.sh --background",
  "ports": [
    {
      "name": "ResQFlow (UI + API)",
      "port": 8000
    }
  ],
  "terminals": [
    {
      "name": "ResQFlow server",
      "command": "./start.sh --background",
      "description": "Starts the unified ResQFlow server on port 8000 (UI + digital twin API)."
    }
  ]
}
